import GuiText from './gui_text';

// Constants
const ORDENATION = { UP: 0, DOWN: 1 };
const EVENTS = { SELECTION: 0 };
const ALIGN = { LEFT: 'left', RIGHT: 'right', CENTER: 'center' };

export { ORDENATION, EVENTS, ALIGN, GuiText };
